config:
  target: 'http://web-ui-coolstore-ui-test.apps.na39.openshift.opentlc.com'
  phases:
    - duration: 10
      arrivalRate: 20
  defaults:
    headers:
      x-my-service-auth: ''
scenarios:
  - flow:
    - get:
        url: "/"
